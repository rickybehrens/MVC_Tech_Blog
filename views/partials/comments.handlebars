<div class="comments">
  {{#each this}}
  <section class="comments">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comments_text}}
    </div>
  </section>
  {{/each}}
</div>
